#include <stdio.h>
/**
 * main - entry point
 * Description: numbers
 * Return: always zero (success)
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
